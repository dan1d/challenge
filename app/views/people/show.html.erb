<h1><%= @person.full_name %></h1>
<%= react_component("Person", props: { person: @person.as_json(methods: [:as_director, :as_producer, :as_actor_actress, :full_name, :tag_names]) }, prerender: false) %>
